import { FiltroTextoPipe } from './filtro-texto.pipe';

describe('FiltroTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
