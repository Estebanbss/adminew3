import { BotonServiciosPipe } from './boton-servicios.pipe';

describe('BotonServiciosPipe', () => {
  it('create an instance', () => {
    const pipe = new BotonServiciosPipe();
    expect(pipe).toBeTruthy();
  });
});
