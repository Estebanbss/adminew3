import { BotonMunicipioPipe } from './boton-municipio.pipe';

describe('BotonMunicipioPipe', () => {
  it('create an instance', () => {
    const pipe = new BotonMunicipioPipe();
    expect(pipe).toBeTruthy();
  });
});
