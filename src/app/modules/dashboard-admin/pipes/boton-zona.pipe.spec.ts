import { BotonZonaPipe } from './boton-zona.pipe';

describe('BotonZonaPipe', () => {
  it('create an instance', () => {
    const pipe = new BotonZonaPipe();
    expect(pipe).toBeTruthy();
  });
});
